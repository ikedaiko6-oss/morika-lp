# morika-lp

「杜花（もりか）| 掌焙煎珈琲」のランディングページ。単一の `index.html` で構成。

## 作業開始時のルール

作業を始める前に、必ず `git fetch origin` してリモート（GitHub）との差分を確認すること。
ローカルの作業ツリーが `origin/main` より古い場合は、作業前にユーザーに知らせる（黙ってpullしない）。

複数デバイス（Windows/Mac/iPad等）から同じセッションやリポジトリにアクセスする運用のため、
「自分が見ているのが最新かどうか」を毎回人間が覚えておかなくても良いようにするための仕組み。
